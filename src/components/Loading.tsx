const Loading = () => (
  <div className=" text-xl h-screen w-screen opacity-80 z-10 top-0 bottom-0 left-0 bg-slate-300 overflow-hidden block flex items-center justify-center fixed">
    Loading
  </div>
);

export default Loading;
